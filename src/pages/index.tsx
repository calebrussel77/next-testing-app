import type { NextPage } from 'next';
import Counter from '@components/counter/counter';

const Home: NextPage = () => {
  return (
    <div>
      <Counter />
    </div>
  );
};

export default Home;
